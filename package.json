{
  "name": "playwright_apitesting_repo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "echpo 'No dev script specified'",
    "mock:start": "node mock-server.js",
    "test": "npx playwright test",
    "test:mock": "echo 'Start mock server in another terminal with `npm run mock:start` then run `npm test`'",
    "test:mock-run": "node scripts/run-mock-tests.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",  
  "devDependencies": {
    "@faker-js/faker": "^10.3.0",
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.2.0"
  },
  "dependencies": {
    "luxon": "^3.7.2"
  }
}
